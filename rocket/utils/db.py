import pypyodbc as db

def get_tablenames(con):
    ''' gets the tablenames from the database via con'''
    pass

def create_table(con, tablename, col_defs, keys=None):
    ''' creates the table in the database via con
        it must know the name and what the columsn will be like. 

        col_defs should be like: IDK!!!!!!

        keys should be like: IDK!!!!!
    '''
    pass