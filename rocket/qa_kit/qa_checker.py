# This qa checker will do the qa at the end of the convert. It can also be added as an independent functionality
# The first priority is to check the duplicate GUID in the data
# Duplicate URSI

# The second is to check whether any data exists


# The design is probably